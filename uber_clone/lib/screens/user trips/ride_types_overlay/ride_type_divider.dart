import 'package:flutter/material.dart';

class RideTypeDivider extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: 0.5,
      color: Colors.grey[700],
    );
  }
}
